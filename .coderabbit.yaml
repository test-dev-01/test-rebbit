# .coderabbit.yaml
language: en
# Enables advanced reviews
review:
  enabled: true
  # Only trigger PRs targeting these branches
  include_branches:
    - main
    - dev
# Optional: Set up specific rules or ignore patterns
